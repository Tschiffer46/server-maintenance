# Hermes infrastruktur-övervakning
# Uppdatera värden här – kör sedan phase4-setup-infra.sh på servern.

# === Mailcow (körs på samma server) ===
MAILCOW_HOST=127.0.0.1
MAILCOW_HTTP_PORT=81
# Portarna som Mailcow ska lyssna på
MAILCOW_PORTS="25 465 587 993"

# === Web-hosting-prod ===
WEBHOSTING_HOST=89.167.90.112
WEBHOSTING_SSH_USER=deploy
WEBHOSTING_SSH_KEY=/home/hermes/.ssh/id_ed25519_webhosting

# === Tröskelvärden ===
# Varna om disk-användning överstiger WARN, kritisk varning vid CRIT
DISK_WARN_PERCENT=80
DISK_CRIT_PERCENT=90
# Varna om RAM-användning överstiger dessa nivåer
MEM_WARN_PERCENT=85
MEM_CRIT_PERCENT=95
